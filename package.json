{
  "name": "walkin-node-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rmdir /q/s build && cd react-front && npm run build --prod && xcopy /E build ..\\build\\",
    "copy-build-folder": "rmdir /q/s build && cd react-front && xcopy /E build ..\\build\\",
    "build:dummy": "echo 'echo from npm script'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^3.2.0",
    "axios": "^0.20.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "node-fetch": "^2.6.1",
    "npm-run-all": "^4.1.5",

    "request": "^2.88.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.5"
  }
}
